node_modules/
.next/
out/
build/
dist/
*.tsbuildinfo
next-env.d.ts

# Secrets — MASTER_KEY lives here. Losing this file makes every journal
# entry permanently unrecoverable; leaking it removes one of the two
# factors protecting them.
.env
.env.*
!.env.example
# Development configuration, committed deliberately: it holds a published key
# and a scratch database. See the header in the file itself.
!.env.development

# Encrypted attachment blobs and database volumes
/data/
/uploads/
*.pgdump

.DS_Store
coverage/
